package abstract_class;

public class AbstractClass {
    //?Pratik Sherdiwala (Codelytics)
    /*
    * => abstract keyword is used to declare incomplete member in a class.
    * => we can declare only methods as an abstract
    * => We cant create an instance/object of abstract class but we can
    * declare constructor inside abstract class.
    * => When a class' members are incomplete/abstract then that class is also
    * incomplete/abstract.
    * => In a class where all the incomplete methods are going to get complete,
    * it is known as Concrete class and we can create an instance of Concrete class.
    * */
}
